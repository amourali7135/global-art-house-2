Random artists from random countries and sorted by various categories to display.Grid cards by:  country
3-4X in one row for each test.

<%= render partial: 'shared/banner'  %>


<div class="container">
  <div class="row">
  <h3>Explore art all around the world</h3>
  <h3>Click a country on the map or search</h3>
  </div>
</div>

<div class="container">
  <div class="row">
   <%= link_to "Browse Art", browse_path, class: 'btn btn-primary' %>
  </div>
</div>

 <%= simple_form_for(:country, as: :select2, method: :get, url: '/artists_countries') do |f| %>
  <%= f.input :country, as: :select2, collection: CountryStateSelect.countries_collection.sort %>
  <%= f.submit %>
<% end %>

<div

  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"

></div>

<section class="clean-block clean-catalog dark">
<div class="container">
  <div class="block-heading">
    <h2 class="text-info">Browse our artists</h2>
  </div>


A small selection of some of our artists!

<div class="container", id="front-page-grid">
  <% @artists.sample(3).each do |artist| %>
    <div class="row">
      <div class=" products pl-5 card-grid-3c">
        <%= render partial: 'shared/home_artist_card', locals: {artist: artist} %>
      </div>
    </div>
  <% end %>
</div>

Grid test II to generate random arts.  Why all Piter/SM though?  Controller is cause.

<div class="container">
  <% @arts.sample(3).each do |art| %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_art_card', locals: {art: art} %>
      </div>
    </div>
  <% end %>
</div>

Our most like arts!
<div class="container">
    <% @arts.sort_by { |art| -art.get_likes.size }.first(3).each do |art|  %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_art_card', locals: {art: art} %>
      </div>
    </div>

  <% end %>
</div>

Our most liked artists.
<div class="container">
    <% @artists.sort_by { |artist| -artist.get_likes.size }.first(3).each do |artist|  %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_artist_card', locals: {artist: artist} %>
      </div>
    </div>
  <% end %>
</div>

Our most followed artists.
<div class="container">
  <% @artists.sort_by { |artist| -artist.followers_count }.first(3).each do |artist|  %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_artist_card', locals: {artist: artist} %>
      </div>
    </div>
  <% end %>
</div>

Our most commented on arts.
<div class="container">
  <% @arts.sort_by { |art| -art.comments.count }.first(3).each do |art|  %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_art_card', locals: {art: art} %>
      </div>
    </div>
  <% end %>
</div>

Our most viewed arts.
<div class="container">
  <% @arts.sort_by { |art| -art.hits }.first(3).each do |art|  %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_art_card', locals: {art: art} %>
      </div>
    </div>
  <% end %>
</div>

Our most viewed artists.
<div class="container">
  <% @artists.sort_by { |artist| -artist.hits }.first(3).each do |artist|  %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_artist_card', locals: {artist: artist} %>
      </div>
    </div>
  <% end %>
</div>

Grid test VII artists by most recent negative - won't work here, why?  Putin won't show when loggedin.
method = artist.created_at  only one that own't work.

<div class="container">
  <% @artists.sort_by { |artist| artist.created_at }.last(3).each do |artist|  %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_artist_card', locals: {artist: artist} %>
      </div>
    </div>
  <% end %>
</div>


Most recently added art.
<div class="container">
  <% @arts.sort_by { |art| art.created_at }.last(3).each do |art|  %>
    <div class="row">
      <div class="col-sm-6">
        <%= render partial: 'shared/home_art_card', locals: {art: art} %>
      </div>
    </div>
  <% end %>
</div>

Grid test IX Artists of a random country...


 </div>
  </div>
</div>
</section>
