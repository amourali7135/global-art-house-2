Make conditional if user is not an artist only for it to show!
Show art they have,add new art, edit existing art, see followed shit.
If statement for different shit to show!

<div class="container">
  <div class="row">
    <div class="class">
      <h1>Welcome to Global Art House!</h1>
      <h3>Artist? Register here!</h3>
      <%= link_to "new", new_artist_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>


<% if current_user.@artist? %>
<h1>  Show your art to the world</h1>
<%= link_to "Add Art", new_artist_art_path(@artist.id), class: 'btn btn-secondary' %>
<% elsif current_user.nil? %>
<h2>Hello!<h2>
<% else %>%
<% end %>
