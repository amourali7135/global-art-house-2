How to sort by updated at?  Message updated at!  So deep though.
<h1 class="row align-items-center justify-content-center">Your conversations</h1>
<div class="columns">

  <div class="column is-7">
    <h3 class="title is-3 row align-items-center justify-content-center">Conversations</h3>
    <%# @conversations.each do |conversation| %>
    <% @conversations.each do |conversation| %>
      <% if conversation.author_id == current_user.id || conversation.receiver_id == current_user.id %>
        <% if conversation.author_id == current_user.id %>
          <% recipient = User.find(conversation.receiver_id) %>
        <% else %>
          <% recipient = User.find(conversation.author_id) %>
        <% end %>
        <% unless current_user.id == recipient %>
          <%# <div class="columns">
            <div class="column">
              <div class="inline-block row align-items-center justify-content-center"><%= link_to recipient.username, conversation_messages_path(conversation) %>
            <%# </div> %>
          <%# </div> %>
              <div class="container row align-items-center justify-content-center">
            <div class="row">
           <%# @conversations.each do |conversation| %>
          <%= render partial: 'conversation', locals: {conversation: conversation} %>
          <% end %>
          </div>
          </div>
        <% end %>
      <% end %>
    <%# <% end %>
  <%# </div> %>
</div>
</div>

<% Conversation.whatever %>




